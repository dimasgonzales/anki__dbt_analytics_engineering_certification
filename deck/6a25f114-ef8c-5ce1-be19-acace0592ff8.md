---
tags:
- card_type/factual
- development/sources
- testing/test-config
citations:
- https://docs.getdbt.com/reference/resource-properties/freshness
guid: 93f1c9ae70
source: llm
uuid: 6a25f114-ef8c-5ce1-be19-acace0592ff8
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly defines `warn_after` and `error_after` as the configuration keys in the freshness block for timing thresholds, and specifies that each requires a count and period parameter."
  citation:
    evidence_source: "https://docs.getdbt.com/reference/resource-properties/freshness"
    quote: "* `warn_after`: {count: <positive_integer>, period: minute | hour | day} * `error_after`: {count: <positive_integer>, period: minute | hour | day}"
    is_quote_valid: true
---

<front>

Which two configuration keys are required within a source `freshness` block to define the timing thresholds for alerts?

</front>

---

<back>

The keys are `warn_after` and `error_after`. Both require a `count` (integer) and a `period` (e.g., minute, hour, day) to function.

```yaml
freshness:
  warn_after: {count: 12, period: hour}
  error_after: {count: 24, period: hour}
```

</back>
