---
tags:
- card_type/factual
- development/modularity
- development/seeds
- pipelines/dag-failures
citations:
- cleaned_docs/seeds/rank_1.md
guid: 3276adfc5e
source: llm
uuid: f541bbad-b16a-5e04-a73f-b695622985a6
claim_meta:
  verdict: NOT_FOUND
  explanation: "The reference text discusses seeds in dbt, including configuration, building with --select, hooks, and the dbt build command, but it does not mention how to reference seeds within models using the `ref()` function or any other method for tracking dependencies in the DAG."
  citation:
    quote: null
    is_quote_valid: false
---

<front>

How do you reference a seed within a dbt model to ensure the dependency is tracked in the DAG?

</front>

---

<back>

Use the `ref()` function, treating the seed exactly like a model:

```sql
select * 
from {{ ref('seed_file_name') }}
```

</back>
