---
tags:
- card_type/scenario
- materializations
- project-config
- sources
citations:
- cleaned_docs/python-models/rank_9.md
guid: a366f920fb
source: llm
uuid: ef31647f-8c3b-5a6d-9fb7-1f674853fcb3
---

<front>

Given the following configuration, how will the `vip_customers` model be materialized?

**dbt_project.yml:**
```yaml
models:
  my_project:
    marts:
      +materialized: table
```

**models/marts/vip_customers.sql:**
```sql
{{ config(materialized='view') }}
select * ...
```

</front>

---

<back>

It will be materialized as a **view**.

The in-file `{{ config() }}` block always overrides configurations set in `dbt_project.yml`, even if the YAML config is specific to that folder.

</back>
