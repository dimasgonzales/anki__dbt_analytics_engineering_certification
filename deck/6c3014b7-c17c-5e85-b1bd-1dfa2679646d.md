---
tags:
- card_type/factual
- development/python-models
citations:
- cleaned_docs/python-models/rank_1.md
guid: c42ffc1f34
source: llm
uuid: 6c3014b7-c17c-5e85-b1bd-1dfa2679646d
claim_meta:
  verdict: SUPPORTED
  explanation: "The answer is explicitly supported by the reference text, which provides a code example demonstrating the exact function signature and return type described."
  citation:
    quote: "def model(dbt, session):"
    is_quote_valid: true
---

<front>

What function signature is required to define a dbt Python model?

</front>

---

<back>

A dbt Python model must be defined by a function named `model` that accepts exactly two arguments: `dbt` and `session`. It must return a final DataFrame.

```python
def model(dbt, session):
    # logic here
    return df
```

</back>
