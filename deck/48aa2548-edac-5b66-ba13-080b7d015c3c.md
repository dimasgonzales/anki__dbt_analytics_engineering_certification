---
tags:
- card_type/factual
- development/jinja-macros
- development/project-config
citations:
- cleaned_docs/dag-failures/rank_9.md
guid: 155e25f6bd
source: llm
uuid: 48aa2548-edac-5b66-ba13-080b7d015c3c
claim_meta:
  verdict: SUPPORTED
  explanation: "The Reference Text explicitly states that the in-file config() macro takes precedence over configurations in dbt_project.yml, directly supporting the answer's claim about precedence."
  citation:
    evidence_source: "https://docs.getdbt.com/reference/define-configs"
    quote: "if you have a `config()` block in your model or directory of models, it will take precedence over any config defined in the project file."
    is_quote_valid: true
---

<front>

How does dbt handle configuration precedence between `dbt_project.yml` and the in-file `{{ config() }}` macro?

</front>

---

<back>

The in-file `{{ config() }}` macro always takes precedence over configurations defined in `dbt_project.yml`. This allows you to set global defaults at the project or directory level while overriding specific settings for individual models.

</back>
