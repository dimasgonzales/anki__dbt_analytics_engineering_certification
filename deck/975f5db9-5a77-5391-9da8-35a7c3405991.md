---
tags:
- card_type/factual
- development/seeds
- governance/contracts
- pipelines/dag-failures
citations:
- cleaned_docs/state-concepts/rank_9.md
guid: 3034ca8fd7
source: llm
uuid: 975f5db9-5a77-5391-9da8-35a7c3405991
claim_meta:
  verdict: NOT_FOUND
  explanation: "The reference text discusses Jinja usage in YAML files and lists various Jinja functions, but it does not mention the `source()` function or provide information on referencing raw data in YAML for building the dependency graph."
  citation:
    quote: null
    is_quote_valid: false
---

<front>

Which Jinja function is required to reference raw data defined in YAML to ensure dbt can build the dependency graph?

</front>

---

<back>

The `source()` function. It replaces hardcoded schema/table names and takes two arguments: the source name and the table name.

Example:
```sql
FROM {{ source('stripe', 'payments') }}
```

</back>
