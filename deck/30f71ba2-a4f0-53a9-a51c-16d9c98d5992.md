---
tags:
- card_type/factual
- debugging/compiled-code
- development/cli-commands
- governance/contracts
citations:
- cleaned_docs/snapshots/rank_8.md
guid: 69519edae3
source: llm
uuid: 30f71ba2-a4f0-53a9-a51c-16d9c98d5992
claim_meta:
  verdict: CONTRADICTED
  explanation: "The answer correctly states that dbt saves failing records to a table when using the `--store-failures` flag, but it inaccurately specifies the schema name as 'dbt_test__audit'. The reference text indicates the default schema is '{{ profile.schema }}_dbt_test__audit', which includes a dynamic prefix based on the profile, contradicting the simplified schema name provided in the answer."
  citation:
    evidence_source: "https://docs.getdbt.com/reference/resource-configs/store_failures"
    quote: "By default, `store_failures` uses a schema named `{{ profile.schema }}_dbt_test__audit`"
    is_quote_valid: true
---

<front>

What happens when you run dbt tests with the `--store-failures` flag?

</front>

---

<back>

dbt runs the test queries and saves the **actual failing records** into a table in the database (typically in a schema named `dbt_test__audit`). This allows you to query the table directly to analyze the bad data that caused the test to fail.

</back>
