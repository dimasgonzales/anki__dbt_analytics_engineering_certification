---
tags:
- card_type/factual
- development/python-models
- development/snapshots
citations:
- cleaned_docs/compiled-code/rank_8.md
guid: c4b80261dd
source: llm
uuid: e80f0860-9690-5f3c-b7cc-6424abd9d9b1
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text provides explicit code examples that match the answer's description: 'partition_by' is configured as a dictionary with keys like 'field', 'data_type', and 'granularity', while 'cluster_by' is configured as a string or a list of column names."
  citation:
    quote: "partition_by = { \"field\": \"created_at\", \"data_type\": \"timestamp\", \"granularity\": \"day\" } and cluster_by = [\"customer_id\", \"order_id\"]"
    is_quote_valid: false
---

<front>

What is the syntactic difference between configuring `partition_by` and `cluster_by` for BigQuery models in dbt?

</front>

---

<back>

`partition_by` requires a dictionary defining the `field`, `data_type`, and `granularity`. `cluster_by` requires a list (or string) of column names to sort by.

Example:
```python
config(
  partition_by={'field': 'created_at', 'data_type': 'timestamp'},
  cluster_by=['user_id', 'status']
)
```

</back>
