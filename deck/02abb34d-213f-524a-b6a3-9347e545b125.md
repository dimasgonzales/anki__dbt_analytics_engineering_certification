---
tags:
- card_type/factual
- development/snapshots
- pipelines/dbt-clone
- state/selectors
citations:
- cleaned_docs/packages/rank_10.md
guid: 62444f2eea
source: llm
uuid: 02abb34d-213f-524a-b6a3-9347e545b125
claim_meta:
  verdict: SUPPORTED
  explanation: "The answer accurately reflects the recommended strategy in the reference text, which advises creating one configuration file per directory for model configuration files in a dbt project."
  citation:
    quote: "As in the example above, create a `_[directory]__models.yml` per directory in your models folder that configures all the models in that directory."
    is_quote_valid: true
---

<front>

What is the recommended strategy for organizing model configuration (`.yml`) files in a dbt project?

</front>

---

<back>

Create one configuration file per directory (e.g., `_staging__models.yml`). This creates a balance between centralization (not one file per model) and discoverability (not one giant file for the whole project).

</back>
