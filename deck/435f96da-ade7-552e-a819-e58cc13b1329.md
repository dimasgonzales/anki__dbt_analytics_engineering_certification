---
tags:
- card_type/factual
- development/jinja-macros
- development/materializations
- governance/contracts
citations:
- cleaned_docs/project-config/rank_2.md
guid: a52b7c788f
source: llm
uuid: 435f96da-ade7-552e-a819-e58cc13b1329
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly mentions that configurations can be set within a resource's file using the `{{ config() }}` macro, and provides examples consistent with the answer's description of syntax and placement."
  citation:
    quote: "within the resource’s file using the `{{ config() }}` macro."
    is_quote_valid: true
---

<front>

What is the syntax for defining model configurations directly inside a SQL model file?

</front>

---

<back>

Use the `{{ config() }}` Jinja macro, typically placed at the top of the file. It accepts arguments as key-value pairs.

```sql
{{ config(
    materialized='table',
    schema='marketing'
) }}
```

</back>
