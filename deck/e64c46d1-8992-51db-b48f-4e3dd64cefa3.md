---
tags:
- card_type/factual
- development/project-config
- governance/contracts
- testing/test-config
citations:
- cleaned_docs/packages/rank_1.md
guid: c37cd06c54
source: llm
uuid: e64c46d1-8992-51db-b48f-4e3dd64cefa3
claim_meta:
  verdict: SUPPORTED
  explanation: "The Reference Text provides an example showing that package-specific configuration variables, including those for the 'snowplow' package, are placed under a 'vars' block at the root level of dbt_project.yml, scoped to the package name, which directly supports the Answer."
  citation:
    quote: "vars:     snowplow:       'snowplow:timezone': 'America/New_York'       'snowplow:page_ping_frequency': 10       'snowplow:events': \"{{ ref('sp_base_events') }}\"       'snowplow:context:web_page': \"{{ ref('sp_base_web_page_context') }}\"       'snowplow:context:performance_timing': false       'snowplow:context:useragent': false       'snowplow:pass_through_columns': []"
    is_quote_valid: true
---

<front>

You need to change the `star_schema` config variable for the installed `snowplow` package to `false`. Given the `dbt_project.yml` below, where does this config belong?

```yaml
name: my_project
config-version: 2
# ... models config ...
```

</front>

---

<back>

It belongs under a `vars` block at the root level, scoped to the package name.

```yaml
vars:
  snowplow:
    snowplow:star_schema: false
```

</back>
