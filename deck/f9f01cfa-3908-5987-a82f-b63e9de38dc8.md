---
tags:
- card_type/factual
- debugging/compiled-code
- development/cli-commands
- state/selectors
citations:
- cleaned_docs/test-config/rank_9.md
guid: 579933fb5e
source: llm
uuid: f9f01cfa-3908-5987-a82f-b63e9de38dc8
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly lists the '--exclude' flag as one of the arguments that can be used with dbt commands to prevent or exclude specific nodes, supporting the answer."
  citation:
    quote: "Note that the following arguments (`--select`, `--exclude`, and `--selector`) also apply to other dbt tasks, such as `test` and `build`."
    is_quote_valid: true
---

<front>

Which CLI flag allows you to prevent specific packages or models from running during a dbt execution?

</front>

---

<back>

The `--exclude` flag.

It removes the specified nodes from the list of nodes generated by the `--select` criteria.

Example:
```bash
dbt build --exclude package:dbt_project_evaluator
```

</back>
