---
tags:
- card_type/factual
- development/modularity
- development/sql-performance
- governance/model-access
citations:
- cleaned_docs/model-access/rank_1.md
guid: 1ba39f2f1f
source: llm
uuid: 03634939-e3cc-5610-994f-b2391d934ef2
claim_meta:
  verdict: SUPPORTED
  explanation: "The answer correctly lists the three model access levels (private, protected, public) and identifies protected as the default, which is explicitly supported by the reference text. The text confirms the three access modifiers and states that protected is the default setting."
  citation:
    quote: "There are three types of access modifiers: * **Private:** A model with a private access modifier is only referenceable by models within the same group. * **Protected:** Models with a protected access modifier can be referenced by any other model within the same dbt project or when the project is installed as a package. This is the default setting for all models, ensuring backward compatibility, especially when g"
    is_quote_valid: false
---

<front>

What are the three model access levels available in dbt, and which one is the default?

</front>

---

<back>

The three access levels are `private`, `protected`, and `public`. The default level is **protected**.

1. `private`: Valid only within the same group.
2. `protected`: Valid within the same project or package.
3. `public`: Valid across projects (dbt Mesh).

</back>
