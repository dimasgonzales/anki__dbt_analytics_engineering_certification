---
tags:
- card_type/factual
- development/jinja-macros
- development/modularity
- development/sources
citations:
- cleaned_docs/dag-failures/rank_8.md
guid: 77163f9fc8
source: llm
uuid: b21dd170-7fd8-583c-84a6-9399f77dc368
claim_meta:
  verdict: NOT_FOUND
  explanation: "The reference text confirms the existence of a `{{ source() }}` function used in dbt for sourcing tables, but does not explicitly state or provide examples of the exact syntax with source name and table name parameters, nor demonstrate its use in a SELECT statement to select data from a raw table defined in sources.yml."
  citation:
    quote: "Optionally configure whether dbt should quote databases, schemas, and identifiers when resolving a `{{ source() }}` function to a direct relation reference."
    is_quote_valid: true
---

<front>

What is the specific syntax used to select data from a raw table defined in a `sources.yml` file within a dbt model?

</front>

---

<back>

Use the `source()` function with the source name and table name:
```sql
select * from {{ source('source_name', 'table_name') }}
```

</back>
