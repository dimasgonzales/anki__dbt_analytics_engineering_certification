---
tags:
- card_type/factual
- debugging/compiled-code
- development/grants
- state/selectors
citations:
  - docs/site_docs.getdbt.com/citations/docs_getdbt_com_reference_node_selection_methods.md
guid: Q0r57%KqxR
source: apkg
uuid: 9595de99-6a9a-55e0-a49b-a60e62c2b90d
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly describes using the 'config' method with the syntax 'config.<option path>:<value>' in the --select command, matching the answer's format and providing direct examples."
  citation:
    quote: "The `config` method is used to select models that match a specified node config. dbt run --select \"config.materialized:incremental\"    # run all models that are materialized incrementally"
    is_quote_valid: false
---

<front>

How can you select models that match a specified node config?

</front>

---

<back>

```
--select config.<option path>:<value>
```  
For example:  

- `dbt run --select config.materialized:incremental` runs all models that are materialized incrementally
- `dbt ls -s config.grants.select:reporter` lists all models where the "reporter" entity has SELECT privileges

</back>
