---
tags:
- card_type/factual
- debugging/compiled-code
- development/jinja-macros
- development/modularity
citations:
- cleaned_docs/modularity/rank_8.md
guid: d7ed16e097
source: llm
uuid: 26447250-1762-5699-a3af-f8a27191d5f1
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly mentions using 'for loops' as control structures in Jinja for SQL and provides an example of iterating over a list to create subtotal columns for pivoting, which aligns with the answer."
  citation:
    quote: "Use control structures (for example, `if` statements and `for` loops) in SQL; Operate on the results of one query to generate another query, for example: + Return a list of payment methods, to create a subtotal column per payment method (pivot)"
    is_quote_valid: false
---

<front>

Which Jinja control structure is used to iterate over a list of values to dynamically generate repetitive SQL code (such as pivoting columns)?

</front>

---

<back>

The `{% for item in list %} ... {% endfor %}` loop is used. It repeats the code block inside the loop for every item in the provided list context.

</back>
