---
tags:
- card_type/factual
- debugging/compiled-code
- documentation/docs-generation
- governance/contracts
citations:
- cleaned_docs/docs-generation/rank_4.md
guid: 6419d4be05
source: llm
uuid: 5b45e2c1-b3dd-59a8-aef8-ea34ea764d6f
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly confirms that the command 'dbt docs generate' compiles the project to build 'manifest.json' and runs queries against database metadata to produce 'catalog.json', which directly supports the answer."
  citation:
    quote: "\"`dbt docs generate` has two supported subcommands: `generate` and `serve`... The command is responsible for generating your project's documentation website by... 2. Compiling the resources in your project, so that their `compiled_code` will be included in `manifest.json`. 3. Running queries against database metadata to produce the `catalog.json` file.\""
    is_quote_valid: false
---

<front>

Which CLI command is used to produce the `catalog.json` and `manifest.json` files required to render the dbt documentation website?

</front>

---

<back>

The command is:
```bash
dbt docs generate
```
This queries the information schema of the database to build the catalog and compiles the project to build the manifest.

</back>
