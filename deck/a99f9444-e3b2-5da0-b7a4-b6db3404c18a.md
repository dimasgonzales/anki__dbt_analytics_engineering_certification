---
tags:
- card_type/factual
- development/materializations
- development/sql-performance
- pipelines/deployment-jobs
citations:
- cleaned_docs/cli-commands/rank_3.md
guid: 9fe1231ee8
source: llm
uuid: a99f9444-e3b2-5da0-b7a4-b6db3404c18a
claim_meta:
  verdict: SUPPORTED
  explanation: "The Reference Text explicitly describes an atomic swap strategy using a temporary table and transactions, which aligns with the Answer's description of minimizing downtime by building in a temporary table and swapping names atomically."
  citation:
    quote: "begin; create table \"dbt_alice\".\"test_model__dbt_tmp\" as ( select 1 as my_column ); alter table \"dbt_alice\".\"test_model\" rename to \"test_model__dbt_backup\"; alter table \"dbt_alice\".\"test_model__dbt_tmp\" rename to \"test_model\" commit;"
    is_quote_valid: true
---

<front>

Describe the "atomic swap" strategy dbt uses when materializing a table model.

</front>

---

<back>

To minimize downtime, dbt builds the new version of the model in a temporary table first. 

Once the build succeeds, dbt drops the old table and renames the temporary table to the production name (often within a single transaction on supported adapters).

</back>
