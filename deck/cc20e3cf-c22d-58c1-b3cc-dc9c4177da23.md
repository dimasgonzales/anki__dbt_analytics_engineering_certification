---
tags:
- card_type/factual
- debugging/compiled-code
- development/cli-commands
- development/seeds
citations:
- cleaned_docs/cli-commands/rank_1.md
guid: 941c164c23
source: llm
uuid: cc20e3cf-c22d-58c1-b3cc-dc9c4177da23
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly mentions that resources for the 'build' task include run (which in dbt context refers to models), test, seed, and snapshot, matching the answer's list of Models, Tests, Seeds, and Snapshots."
  citation:
    quote: "Select resources to build (run, test, seed, snapshot) or check freshness: `--select`, `-s`"
    is_quote_valid: true
---

<front>

What four resource types are executed when running the `dbt build` command?

</front>

---

<back>

The `dbt build` command executes the following resources in DAG order:
1. Models
2. Tests
3. Seeds
4. Snapshots

It is considered a **Write** command.

</back>
