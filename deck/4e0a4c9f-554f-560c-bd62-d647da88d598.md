---
tags:
- card_type/factual
- development/modularity
- development/project-config
- development/seeds
citations:
- cleaned_docs/project-config/rank_4.md
guid: 1c2f19bbfb
source: llm
uuid: 4e0a4c9f-554f-560c-bd62-d647da88d598
claim_meta:
  verdict: SUPPORTED
  explanation: "The answer is supported by the reference text, which explicitly states that variables are defined under the `vars` config in `dbt_project.yml` (as shown in the quote) and referenced using the `var()` Jinja function in SQL models (as evidenced in the second source with the example `select * from events where event_type = '{{ var(\"event_type\", \"activation\") }}'`)."
  citation:
    evidence_source: "https://docs.getdbt.com/docs/build/project-variables"
    quote: "To define variables in a dbt project, add a `vars` config to your `dbt_project.yml` file."
    is_quote_valid: true
---

<front>

How do you define a global variable in `dbt_project.yml` and how do you reference it in a SQL model?

</front>

---

<back>

Define it under the `vars:` dictionary in `dbt_project.yml` and reference it using the `var()` Jinja function.

**Definition:**
```yaml
vars:
  my_variable: 100
```
**Usage:**
```sql
select * from table where id > {{ var('my_variable') }}
```

</back>
