---
tags:
- card_type/factual
- development/cli-commands
- testing/test-config
citations:
- cleaned_docs/error-logs/rank_8.md
guid: 4afa09b8ce
source: llm
uuid: e4ceae42-b911-5763-9d78-0abc6b7e6c9d
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly states that the CLI flags `--warn-error` and `--warn-error-options` are used to promote warnings, including those from tests with `severity: warn`, to errors during execution."
  citation:
    quote: "* `--warn-error`: Promotes *all* dbt warnings (including test warnings, Jinja warnings, deprecations, and so on.) to errors."
    is_quote_valid: true
---

<front>

What CLI flag is used to treat tests configured with `severity: warn` as errors during execution?

</front>

---

<back>

`--warn-error` (or `--warn-error-options`).

Example:
```bash
dbt test --warn-error
```

</back>
