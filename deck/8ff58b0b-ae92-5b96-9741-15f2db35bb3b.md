---
tags:
- card_type/factual
- development/packages
- pipelines/dag-failures
citations:
- cleaned_docs/packages/rank_2.md
guid: 67a2e4424f
source: llm
uuid: 8ff58b0b-ae92-5b96-9741-15f2db35bb3b
claim_meta:
  verdict: NOT_FOUND
  explanation: "The reference text mentions the `dbt deps` command for installing packages and discusses manually adding dependencies to `packages.yml`, but it does not cover any CLI flag or method for programmatically adding dependencies to the file without manual editing, such as the `--add-package` flag."
  citation:
    quote: null
    is_quote_valid: false
---

<front>

What CLI command allows you to programmatically add a dependency to `packages.yml` and install it without manually editing the file?

</front>

---

<back>

The `dbt deps` command with the `--add-package` flag.

Example:
```bash
dbt deps --add-package dbt-labs/codegen@1.0.0
```

</back>
