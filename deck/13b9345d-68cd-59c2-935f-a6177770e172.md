---
tags:
- card_type/factual
- development/materializations
- development/snapshots
- development/sql-performance
citations:
- cleaned_docs/snapshots/rank_5.md
guid: 5d437f29fd
source: llm
uuid: 13b9345d-68cd-59c2-935f-a6177770e172
claim_meta:
  verdict: CONTRADICTED
  explanation: "The answer lists four primary incremental strategy options, but the reference text explicitly includes five built-in strategies: append, merge, delete+insert, insert_overwrite, and microbatch. The descriptions provided for the four strategies are accurate, but the omission of microbatch contradicts the evidence."
  citation:
    evidence_source: "https://docs.getdbt.com/docs/build/incremental-strategy"
    quote: "Built-in Strategies: append, delete+insert, merge, insert_overwrite, microbatch"
    is_quote_valid: false
--- | --- | | [`append`](/docs/build/incremental-strategy#append) | `get_incremental_append_sql` | | [`delete+insert`](/docs/build/incremental-strategy#deleteinsert) | `get_incremental_delete_insert_sql` | | [`merge`](/docs/build/incremental-strategy#merge) | `get_incremental_merge_sql` | | [`insert_overwrite`](/docs/build/incremental-strategy#insert_overwrite) | `get_incremental_insert_overwrite_sql` | | [`microbatch`](/docs/build/incremental-strategy#microbatch) | `get_incremental_microbatch_sql` |"
    is_quote_valid: true
---

<front>

What are the four primary `incremental_strategy` options in dbt and how do they generally handle existing data?

</front>

---

<back>

1. `merge`: Upserts rows based on a unique key (updates matches, inserts new).
2. `delete+insert`: Deletes rows matching the unique key, then inserts all new rows.
3. `append`: Inserts new rows without checking for duplicates.
4. `insert_overwrite`: Replaces entire partitions in the destination table based on the new data.

</back>
