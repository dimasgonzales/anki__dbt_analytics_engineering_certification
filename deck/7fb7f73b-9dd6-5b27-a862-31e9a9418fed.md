---
tags:
- card_type/factual
- development/project-config
- testing/test-config
- testing/test-types
citations:
- cleaned_docs/test-config/rank_7.md
guid: e5fda9efcc
source: llm
uuid: 7fb7f73b-9dd6-5b27-a862-31e9a9418fed
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly states that for configuring a specific singular test, the config() macro should be used in its SQL definition, which directly supports the answer provided."
  citation:
    quote: "To configure a specific singular test, you should use the `config()` macro in its SQL definition."
    is_quote_valid: true
---

<front>

How do you configure `store_failures` for a specific **singular** data test (`.sql` file)?

</front>

---

<back>

Use the `config()` macro at the top of the SQL file.

Example:
```sql
{{ config(store_failures = true) }}

select ...
```

</back>
