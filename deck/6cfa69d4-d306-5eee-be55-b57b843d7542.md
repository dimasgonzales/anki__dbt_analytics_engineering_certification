---
tags:
- card_type/factual
- development/jinja-macros
- development/python-models
- governance/contracts
citations:
- cleaned_docs/materializations/rank_2.md
guid: 8a163480e7
source: llm
uuid: 6cfa69d4-d306-5eee-be55-b57b843d7542
claim_meta:
  verdict: SUPPORTED
  explanation: "The answer correctly identifies that the Jinja config() macro is used to configure a model's materialization directly in the SQL file, which is explicitly supported by the reference text stating the use of a Jinja config block with the materialized argument."
  citation:
    quote: "We can configure an individual model’s materialization using a **Jinja `config` block**, and passing in the **`materialized` argument**. This tells dbt what materialization to use."
    is_quote_valid: true
---

<front>

How do you configure a specific SQL model's materialization strategy directly within the model file?

</front>

---

<back>

Use the Jinja `config()` macro at the top of the SQL file. This allows you to set properties like `materialized`, `schema`, or `tags` for that specific model.

Example:
```sql
{{ config(
    materialized='table'
) }}
```

</back>
