---
tags:
- card_type/factual
- development/seeds
- governance/contracts
- governance/versioning
citations:
- cleaned_docs/versioning/rank_3.md
guid: c9edbe0da9
source: llm
uuid: d5e514dd-99aa-5cb6-a31a-9e387ed35711
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text confirms that 'latest_version' is the property used to designate the canonical version in model configurations, with a default behavior if not set."
  citation:
    quote: "If not specified for a versioned model, `latest_version` defaults to the largest version identifier."
    is_quote_valid: false
---

<front>

When defining model versions in a `schema.yml` file, which property must be set at the top level of the model configuration to designate the canonical version?

</front>

---

<back>

You must set the `latest_version` property.

This tells dbt which version to use when `ref('model_name')` is called without a specific version argument.

Example:
```yaml
models:
  - name: dim_customers
    latest_version: 2
    versions:
      - v: 1
      - v: 2
```

</back>
