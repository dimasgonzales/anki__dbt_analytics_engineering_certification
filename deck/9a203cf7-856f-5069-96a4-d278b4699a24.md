---
tags:
- card_type/factual
- debugging/compiled-code
- development/cli-commands
- development/seeds
citations:
- cleaned_docs/seeds/rank_8.md
guid: fb84edbbe2
source: llm
uuid: 9a203cf7-856f-5069-96a4-d278b4699a24
claim_meta:
  verdict: SUPPORTED
  explanation: "The Reference Text explicitly states that the dbt seed command loads CSV files from the seed-paths directory (default 'seeds') into the warehouse, and seeds are intended for static, business-specific data, aligning with the Answer."
  citation:
    quote: "The `dbt seed` command will load `csv` files located in the `seed-paths` directory of your dbt project into your data warehouse."
    is_quote_valid: true
---

<front>

What is a dbt seed and what command is used to load it into the warehouse?

</front>

---

<back>

A dbt seed is a CSV file located in the `seeds` directory of a dbt project, intended for static, small reference data. It is loaded into the data warehouse by running the command:
```bash
dbt seed
```

</back>
