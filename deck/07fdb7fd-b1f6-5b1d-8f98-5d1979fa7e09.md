---
tags:
- card_type/factual
- development/materializations
- development/python-models
- testing/test-config
citations:
- cleaned_docs/materializations/rank_9.md
guid: e891a0ed63
source: llm
uuid: 07fdb7fd-b1f6-5b1d-8f98-5d1979fa7e09
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly confirms that in the Materialize dbt adapter, the `table` materialization creates a materialized view (not a physical table) and the `incremental` materialization is unsupported, as stated in the answer."
  citation:
    evidence_source: "https://docs.getdbt.com/docs/core/connect-data-platform/materialize-setup"
    quote: "| `table` | YES | Creates a [materialized view](https://materialize.com/docs/sql/create-materialized-view/#main). (Actual table support pending [#5266](https://github.com/MaterializeInc/materialize/issues/5266)) | | `incremental` | NO | Use the `materializedview` materialization instead. Materialized views will always return up-to-date results without manual or configured refreshes. |"
    is_quote_valid: false
---

<front>

How does the Materialize dbt adapter map the standard `table` and `incremental` materializations?

</front>

---

<back>

In the Materialize adapter, `table` creates a **materialized view** (not a physical table). The `incremental` materialization is **unsupported** and raises an error, as Materialize views inherently maintain up-to-date results without manual incremental logic.

</back>
