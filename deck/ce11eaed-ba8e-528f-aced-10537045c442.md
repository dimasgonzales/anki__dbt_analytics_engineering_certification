---
tags:
- card_type/factual
- development/materializations
- development/project-config
citations:
- cleaned_docs/grants/rank_1.md
guid: f78c7d4bad
source: llm
uuid: ce11eaed-ba8e-528f-aced-10537045c442
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text confirms that when a grant privilege is redefined in a specific model without the '+' prefix, it replaces (clobbers) the project-level configuration entirely, and merging occurs only when the '+' prefix is used."
  citation:
    quote: "If we were granting other privileges, in addition to `select`, and those privilege names lacked the `+` prefix, they would continue to 'clobber' rather than 'add' new grantees."
    is_quote_valid: false
---

<front>

What happens by default if a privilege is defined in `dbt_project.yml` and then redefined for that same privilege in a specific model?

</front>

---

<back>

The specific model configuration **replaces (clobbers)** the project-level configuration entirely. It does not merge the lists unless special syntax is used.

</back>
