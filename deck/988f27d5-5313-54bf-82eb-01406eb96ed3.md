---
tags:
- card_type/factual
- development/modularity
- development/seeds
- development/sources
citations:
- cleaned_docs/sources/rank_10.md
guid: 6ab9dd80da
source: llm
uuid: 988f27d5-5313-54bf-82eb-01406eb96ed3
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text defines the `identifier` property as the table name in the database and states it defaults to the `name` property. Examples show `name` being used in dbt source references (e.g., {{ source('src', 'name') }}), aligning with the answer's description."
  citation:
    evidence_source: "https://docs.getdbt.com/reference/resource-properties/identifier"
    quote: "identifier: The table name as stored in the database. By default, dbt will use the table's `name` parameter as the identifier."
    is_quote_valid: false
---

<front>

In a dbt source definition, what is the difference between the `name` property and the `identifier` property?

</front>

---

<back>

The `name` property defines how you reference the table within dbt (e.g., `{{ source('src', 'name') }}`), while the `identifier` property corresponds to the actual physical table name in the data warehouse. If omitted, the `identifier` defaults to the `name`.

</back>
