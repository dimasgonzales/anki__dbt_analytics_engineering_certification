---
tags:
- card_type/factual
- development/modularity
- development/seeds
- development/sources
citations:
- cleaned_docs/sources/rank_10.md
guid: 6ab9dd80da
source: llm
uuid: 988f27d5-5313-54bf-82eb-01406eb96ed3
claim_meta:
  verdict: NOT_FOUND
  explanation: "The Reference Text supports that the `name` property is used for dbt references and the `identifier` property defines the actual database table name, but it does not contain information about whether `identifier` defaults to `name` when omitted, so the Answer cannot be fully verified."
  citation:
    quote: "use the [schema](/reference/resource-properties/schema) and [identifier](/reference/resource-properties/identifier) properties to define the names as per the database, and use your `name:` property for the name that makes sense!"
    is_quote_valid: true
---

<front>

In a dbt source definition, what is the difference between the `name` property and the `identifier` property?

</front>

---

<back>

The `name` property defines how you reference the table within dbt (e.g., `{{ source('src', 'name') }}`), while the `identifier` property corresponds to the actual physical table name in the data warehouse. If omitted, the `identifier` defaults to the `name`.

</back>
