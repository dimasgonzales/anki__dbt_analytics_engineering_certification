---
tags:
- card_type/factual
- debugging/compiled-code
- development/project-config
- pipelines/deployment-jobs
citations:
- cleaned_docs/python-models/rank_7.md
guid: 6d84eaf230
source: llm
uuid: ddb29b22-c148-5b48-aaf8-81881ad33653
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly mentions 'profiles.yml' as a file used for connecting to the data platform, which involves database connection credentials, thus supporting the answer that credentials and target environments are defined in that file."
  citation:
    quote: "don't need a [`profiles.yml`](/docs/core/connect-data-platform/profiles.yml) file."
    is_quote_valid: true
---

<front>

In which file must you define your database connection credentials (host, user, password) and target environments?

</front>

---

<back>

In the `profiles.yml` file.

This file is separate from the project logic (`dbt_project.yml`) to keep sensitive credentials out of version control. It typically resides in the `~/.dbt/` directory locally.

</back>
