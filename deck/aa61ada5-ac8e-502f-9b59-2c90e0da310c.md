---
tags:
- card_type/factual
- development/python-models
citations:
- cleaned_docs/python-models/rank_4.md
guid: a6c4b09da6
source: llm
uuid: aa61ada5-ac8e-502f-9b59-2c90e0da310c
claim_meta:
  verdict: NOT_FOUND
  explanation: "The Reference Text explicitly shows the function signature 'def model(dbt, session):' which supports part of the answer, but it does not contain information about what the function must return, so the return statement cannot be verified."
  citation:
    quote: "def model(dbt, session):"
    is_quote_valid: true
---

<front>

What is the required function signature for a dbt Python model, and what must it return?

</front>

---

<back>

A dbt Python model must define a function named `model` that takes two arguments, `dbt` and `session`, and returns a final DataFrame.

```python
def model(dbt, session):
    # transformation logic
    return df
```

</back>
