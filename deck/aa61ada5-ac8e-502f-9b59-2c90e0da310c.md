---
tags:
- card_type/factual
- development/python-models
citations:
- cleaned_docs/python-models/rank_4.md
guid: a6c4b09da6
source: llm
uuid: aa61ada5-ac8e-502f-9b59-2c90e0da310c
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly confirms that dbt Python models require a function named 'model' with parameters 'dbt' and 'session', and it must return a DataFrame, which directly supports the answer."
  citation:
    evidence_source: "https://docs.getdbt.com/docs/build/python-models"
    quote: "Each Python model lives in a .py file in your models/ folder. It defines a function named model(), which takes two parameters: dbt and session. The model() function must return a single DataFrame."
    is_quote_valid: false
---

<front>

What is the required function signature for a dbt Python model, and what must it return?

</front>

---

<back>

A dbt Python model must define a function named `model` that takes two arguments, `dbt` and `session`, and returns a final DataFrame.

```python
def model(dbt, session):
    # transformation logic
    return df
```

</back>
