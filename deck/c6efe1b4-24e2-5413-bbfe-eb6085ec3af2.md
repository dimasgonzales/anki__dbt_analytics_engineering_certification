---
tags:
- card_type/factual
- debugging/compiled-code
- development/jinja-macros
- development/packages
citations:
- cleaned_docs/packages/rank_1.md
guid: 217e8ae85b
source: llm
uuid: c6efe1b4-24e2-5413-bbfe-eb6085ec3af2
claim_meta:
  verdict: NOT_FOUND
  explanation: "The Reference Text confirms that `packages.yml` supports Jinja rendering and can use environment variables for secure values like Git tokens, but it does not explicitly mention the `env_var` function or provide enough detail to verify the specific function name."
  citation:
    quote: "packages.yml supports Jinja rendering for historical reasons, allowing dynamic configurations. This can be useful if you need to insert values, like a Git token method from an environment variable, into your package specifications."
    is_quote_valid: false
---

<front>

Which Jinja function allows you to safely pass Git credentials or tokens into `packages.yml` without hardcoding them?

</front>

---

<back>

The `{{ env_var('VARIABLE_NAME') }}` function. 

This allows `packages.yml` to read sensitive values from the system environment variables at runtime.

</back>
