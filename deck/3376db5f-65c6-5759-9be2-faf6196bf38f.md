---
tags:
- card_type/factual
- development/cli-commands
- development/project-config
- testing/test-config
citations:
- cleaned_docs/test-config/rank_7.md
guid: 5f02d629b2
source: llm
uuid: 3376db5f-65c6-5759-9be2-faf6196bf38f
claim_meta:
  verdict: SUPPORTED
  explanation: "The answer is explicitly supported by the reference text, which specifies that for the `store_failures` configuration, setting it to `true` or `false` in YAML or SQL files takes precedence over the CLI flag, and the CLI flag is used only when the config is `none` or omitted."
  citation:
    quote: "If specified as `true` or `false`, the `store_failures` config will take precedence over the presence or absence of the `--store-failures` flag. If the `store_failures` config is `none` or omitted, the resource will use the value of the `--store-failures` flag."
    is_quote_valid: false
---

<front>

How does dbt determine precedence between the `store_failures` configuration in YAML files and the `--store-failures` CLI flag?

</front>

---

<back>

The **configuration in the project files** takes precedence. Explicitly setting `store_failures` to `true` or `false` in YAML or SQL overrides the CLI flag. The CLI flag is only used if the config is omitted or set to `none`.

</back>
