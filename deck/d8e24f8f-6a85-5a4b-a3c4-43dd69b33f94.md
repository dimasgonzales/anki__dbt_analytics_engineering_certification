---
tags:
- card_type/factual
- development/modularity
- development/seeds
- governance/contracts
citations:
- cleaned_docs/test-config/rank_5.md
guid: c32ab134d4
source: llm
uuid: d8e24f8f-6a85-5a4b-a3c4-43dd69b33f94
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly states that when documenting custom test macros in a schema.yml file, the naming convention requires adding the 'test_' prefix to the macro name, which directly supports the answer."
  citation:
    quote: "When documenting custom test macros in a `schema.yml` file, add the `test_` prefix to the macro name. For example, if the test block's name is `not_empty_string`, then the macro's name would be `test_not_empty_string`."
    is_quote_valid: true
---

<front>

When adding documentation for a custom generic test in a `schema.yml` file under the `macros:` key, what naming convention must be used?

</front>

---

<back>

You must prepend `test_` to the generic test's name.

For example, if the test is defined as `{% test is_even ... %}`, the documentation block must reference it as `name: test_is_even`.

</back>
