---
tags:
- card_type/factual
- development/jinja-macros
- development/project-config
citations:
  - docs/site_docs.getdbt.com/citations/docs_getdbt_com_reference_dbt_jinja_functions_profiles_yml_context.md
guid: pDYe5p#OR+
source: apkg
uuid: 3ef281ad-344c-509d-bf6c-5d7668414b05
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text confirms that `env_var` is available in all YAML files that support Jinja, including `profiles.yml`, and that `vars` (or `var`) must be passed via the `--vars` CLI option in `profiles.yml`, aligning with the answer's note."
  citation:
    quote: "You can use `env_var()` in all YAML files that support Jinja. In `dbt_project.yml`, `packages.yml`, and `profiles.yml` files, you must pass `vars` through the CLI using `--vars`, not defined inside the `vars:` block in the YAML file."
    is_quote_valid: false
---

<front>

What Jinja variables/functions are available in the `profiles.yml` context?

</front>

---

<back>

- `env_var`
- `vars` (Note: only variables defined with the --vars CLI option are available)

</back>
