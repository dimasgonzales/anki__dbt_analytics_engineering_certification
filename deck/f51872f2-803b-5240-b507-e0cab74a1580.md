---
citations:
- cleaned_docs/error-logs/rank_6.md
tags:
- card_type/factual
- development/packages
- development/project-config
- development/python-models
uuid: f51872f2-803b-5240-b507-e0cab74a1580
claim_meta:
  verdict: NOT_FOUND
  explanation: "The reference text mentions that `dbt debug` can be used to check connections and debug projects, but it does not specify the five sequential checks listed in the answer, such as `dbt_project.yml` validity, `profiles.yml` configuration, system dependencies, adapter installation, and database connectivity in sequence."
  citation:
    evidence_source: "https://docs.getdbt.com/guides/debug-errors"
    quote: "Use `dbt debug` to check if you can connect to your warehouse."
    is_quote_valid: false
---


<front>

What five primary checks does the `dbt debug` command perform sequentially?

</front>

---

<back>

The `dbt debug` command validates:
1. `dbt_project.yml` validity
2. `profiles.yml` configuration
3. System dependencies (Git, Python)
4. Adapter installation
5. Database connectivity

</back>