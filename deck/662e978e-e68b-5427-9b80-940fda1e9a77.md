---
tags:
- card_type/factual
- debugging/compiled-code
- development/cli-commands
- state/selectors
citations:
- cleaned_docs/selectors/rank_6.md
guid: dc19cf422e
source: llm
uuid: 662e978e-e68b-5427-9b80-940fda1e9a77
claim_meta:
  verdict: NOT_FOUND
  explanation: "The reference text mentions the `default` flag as an optional part of selector definitions in `selectors.yml` and provides examples with `default: true`, but it does not explicitly describe the function of this flag or confirm that it causes the selector to be applied automatically when no explicit node selection flags are provided."
  citation:
    quote: "can optionally define a `description` and [`default` flag](#default)."
    is_quote_valid: true
---

<front>

What is the function of setting `default: true` for a specific selector in `selectors.yml`?

</front>

---

<back>

It marks that selector to be applied automatically when a dbt command (like `dbt run` or `dbt build`) is executed without any explicit node selection flags provided.

</back>
