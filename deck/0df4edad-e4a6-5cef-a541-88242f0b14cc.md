---
tags:
- card_type/factual
- development/materializations
- state/selectors
- testing/test-types
citations:
- cleaned_docs/project-config/rank_2.md
guid: b8b22c36ed
source: llm
uuid: 0df4edad-e4a6-5cef-a541-88242f0b14cc
claim_meta:
  verdict: SUPPORTED
  explanation: "The Reference Text confirms that the `unique_key` can be defined in a configuration block at the top of the model's SQL file using the `config` macro, which aligns with the answer provided."
  citation:
    quote: "You can define `unique_key` in a configuration block at the top of your model, and it can be a single column name or a list of column names."
    is_quote_valid: true
---

<front>

You are building an incremental model and need to define the `unique_key` for row updates. How would you configure this within the model file itself?

</front>

---

<back>

Add the `unique_key` argument to the `config` macro inside the model's SQL file.

```sql
{{ config(
    materialized='incremental',
    unique_key='transaction_id'
) }}

SELECT ...
```

</back>
