---
tags:
- card_type/factual
- development/jinja-macros
- documentation/docs-generation
citations:
- cleaned_docs/model-access/rank_8.md
guid: 839c329507
source: llm
uuid: 8547fa72-a5b2-5657-9950-f7612cb7619c
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly states that the 'quote' field enables or disables quoting for column names, directly supporting the answer's claim that setting 'quote: true' forces dbt to wrap column names in quotes. The additional context about reserved keywords is a logical extension but not explicitly addressed in the text."
  citation:
    evidence_source: "https://docs.getdbt.com/reference/resource-properties/columns"
    quote: "The `quote` field can be used to enable or disable quoting for column names."
    is_quote_valid: true
---

<front>

When defining column properties in YAML, what does setting `quote: true` accomplish?

</front>

---

<back>

It forces dbt to wrap the column name in quotes (e.g., `"order"` vs `order`) in the generated SQL.

This is necessary when a column name conflicts with a database reserved keyword (like `group`, `user`, or `order`).

</back>
