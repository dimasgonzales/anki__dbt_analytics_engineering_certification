---
tags:
- card_type/factual
- debugging/error-logs
- development/python-models
- testing/test-config
citations:
- cleaned_docs/materializations/rank_4.md
guid: bc59cfffd0
source: llm
uuid: 896ba911-df42-5943-a81c-c227b1fa6eb9
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly states that config.get is for optional configurations with a default, and config.require is for required configurations, which directly supports the answer's description of their differences in the context of custom materializations."
  citation:
    evidence_source: "https://docs.getdbt.com/guides/create-new-materializations"
    quote: "The `get` function is used to retrieve an optional configuration option, while the `require` function is used to specify a required configuration option."
    is_quote_valid: true
---

<front>

In the context of custom materializations, what is the difference between `config.get()` and `config.require()`?

</front>

---

<back>

**`config.get('key', default)`** retrieves a configuration value if it exists, or uses a fallback value if provided, allowing for optional configs.

**`config.require('key')`** attempts to retrieve the value but raises a **compilation error** if the user has not defined that configuration key in their model.

</back>
