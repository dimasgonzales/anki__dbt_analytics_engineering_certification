---
tags:
- card_type/factual
- development/modularity
- development/seeds
- governance/contracts
citations:
- cleaned_docs/sources/rank_2.md
guid: 6ed1c1bae3
source: llm
uuid: 87e2f49e-4266-5ffe-a70f-6b9bc9861c4a
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text defines the logical name (e.g., 'name' under 'tables') as the identifier to use in the {{ source() }} function, consistent with the answer. It shows examples of referencing source tables without using physical identifiers, supporting the instruction to use logical names and avoid physical references directly in SQL."
  citation:
    quote: "from {{ source('jaffle_shop', 'customers') }}"
    is_quote_valid: true
---

<front>

You have defined a source table with the logical name `users` and the physical identifier `RAW_USERS_V2`. How must you reference this table in your model SQL files?

</front>

---

<back>

You must use the logical name inside the `{{ source() }}` function. Do not reference the physical identifier or the physical schema directly in the SQL.

```sql
-- Correct usage
select * from {{ source('my_source', 'users') }}
```

</back>
