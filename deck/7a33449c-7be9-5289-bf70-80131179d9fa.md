---
tags:
- card_type/factual
- development/jinja-macros
- development/python-models
- governance/model-access
citations:
- cleaned_docs/python-models/rank_1.md
guid: 9d2be9b3a9
source: llm
uuid: 7a33449c-7be9-5289-bf70-80131179d9fa
claim_meta:
  verdict: SUPPORTED
  explanation: "The answer is supported by the reference text, which shows that in SQL models, the `is_incremental()` macro is used (e.g., in the example with `{% if is_incremental() %}`), and for Python models, it explicitly mentions accessing `dbt.is_incremental` on the `dbt` object to check the incremental state."
  citation:
    quote: "Determining if the current model's run is incremental: `dbt.is_incremental`"
    is_quote_valid: true
---

<front>

How is the syntax for checking incremental state different in a Python model compared to a SQL model?

</front>

---

<back>

In SQL models, you use the Jinja macro `{{ is_incremental() }}`. In Python models, you access the boolean attribute `dbt.is_incremental` on the `dbt` object.

```python
if dbt.is_incremental:
    # logic
```

</back>
