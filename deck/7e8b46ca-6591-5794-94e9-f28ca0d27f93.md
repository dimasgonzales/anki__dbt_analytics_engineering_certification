---
tags:
- card_type/factual
- development/cli-commands
- documentation/descriptions
citations:
- cleaned_docs/grants/rank_7.md
guid: c3d8035b34
source: llm
uuid: 7e8b46ca-6591-5794-94e9-f28ca0d27f93
claim_meta:
  verdict: SUPPORTED
  explanation: "The Reference Text explicitly confirms that the `snapshot_meta_column_names` configuration is used to rename default meta-columns in dbt snapshots, directly supporting the answer."
  citation:
    quote: "All of these column names can be customized using the `snapshot_meta_column_names` config."
    is_quote_valid: true
---

<front>

Which configuration dictionary allows you to rename the default meta-columns (like `dbt_valid_from` and `dbt_valid_to`) generated by dbt snapshots?

</front>

---

<back>

Use the `snapshot_meta_column_names` configuration. Within this dictionary, map the internal dbt key (e.g., `dbt_valid_to`) to your desired column name.
```yaml
config:
  snapshot_meta_column_names:
    dbt_valid_to: effective_end_date
```

</back>
