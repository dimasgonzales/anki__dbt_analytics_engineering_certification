---
tags:
- card_type/factual
- development/cli-commands
- development/jinja-macros
- development/materializations
citations:
- cleaned_docs/snapshots/rank_9.md
guid: b4233ab2a3
source: llm
uuid: 00926d43-8064-5b2d-9b20-657a0fe5b133
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly lists the same three conditions for the `is_incremental()` macro to return true as provided in the answer, with no contradictions."
  citation:
    quote: "The `is_incremental()` macro will return `True` if *all* of the following conditions are met:  * The model must already exist as a table in the database * The `full-refresh` flag *is not* passed * The running model is configured with `materialized='incremental'`"
    is_quote_valid: false
---

<front>

What three specific conditions must be met for the `is_incremental()` macro to return `True`?

</front>

---

<back>

1. The destination table already exists in the database.
2. The run is NOT being executed with the `--full-refresh` flag.
3. The model configuration is set to `materialized='incremental'`.

</back>
