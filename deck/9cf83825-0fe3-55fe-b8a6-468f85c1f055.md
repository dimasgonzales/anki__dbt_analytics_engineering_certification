---
tags:
- card_type/factual
- debugging/compiled-code
- development/jinja-macros
- development/python-models
citations:
- cleaned_docs/jinja-macros/rank_9.md
guid: 2228b2d75c
source: llm
uuid: 9cf83825-0fe3-55fe-b8a6-468f85c1f055
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text explicitly states that using 'do return(...)' in a macro returns the value as a list without outputting a string, which directly supports the answer that 'codes' is a list and 'codes is string' is false."
  citation:
    quote: "In the following example ,`{% do return([1,2,3]) %}` acts as a *statement* that executes the return action but does not output a string:"
    is_quote_valid: true
---

<front>

You have a macro `get_status_codes()` that contains `{% do return([200, 400, 500]) %}`. 

In a model, you run `{% set codes = get_status_codes() %}`. If you check `{{ codes is string }}`, will it be true or false?

</front>

---

<back>

It will be **false**. 

Because the macro uses `return` inside a `do` block, the variable `codes` is set to an actual Python **list**, not a string representation of that list.

</back>
