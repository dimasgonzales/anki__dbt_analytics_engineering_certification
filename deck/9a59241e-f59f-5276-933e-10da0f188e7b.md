---
tags:
- card_type/factual
- development/jinja-macros
- development/modularity
- development/seeds
citations:
- cleaned_docs/yaml-compilation/rank_3.md
guid: cddbb8b19d
source: llm
uuid: 9a59241e-f59f-5276-933e-10da0f188e7b
claim_meta:
  verdict: SUPPORTED
  explanation: "The reference text confirms that the dbt templater in SQLFluff compiles dbt code, which includes Jinja syntax, into data warehouse SQL before linting, directly supporting the answer's explanation of why it's required."
  citation:
    evidence_source: "https://docs.getdbt.com/docs/cloud/dbt-cloud-ide/lint-format"
    quote: "# This change (from Jinja to dbt templater) will make linting slower because linting will first compile dbt code into data warehouse code."
    is_quote_valid: false
---

<front>

Why is the `dbt` templater required when using SQLFluff with a dbt project?

</front>

---

<back>

Standard SQL linters cannot interpret Jinja syntax like `{{ ref() }}` or `{{ config() }}`. The `dbt` templater compiles the Jinja code into valid data warehouse SQL before the linter analyzes it.

Configure it in `.sqlfluff`:
```ini
[sqlfluff]
templater = dbt
```

</back>
