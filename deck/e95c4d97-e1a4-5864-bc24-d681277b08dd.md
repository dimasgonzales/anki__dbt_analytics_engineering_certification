---
tags:
- card_type/factual
- debugging/error-logs
- development/jinja-macros
citations:
- cleaned_docs/versioning/rank_7.md
guid: c1fb87768d
source: llm
uuid: e95c4d97-e1a4-5864-bc24-d681277b08dd
claim_meta:
  verdict: SUPPORTED
  explanation: "The Reference Text explicitly describes the 'log()' function in dbt macros, specifying that the 'info' parameter controls whether messages are written to stdout. Setting 'info=True' ensures the message appears in the CLI console, matching the answer."
  citation:
    quote: ":param info: If `False`, write to the log file. If `True`, write to both the log file and stdout."
    is_quote_valid: true
---

<front>

When using the `log()` function in a dbt macro, what argument must be set to ensure the message appears in the CLI console (stdout)?

</front>

---

<back>

You must set `info=true`.

By default, the `info` argument is `false`, meaning messages are written only to the `dbt.log` file and are invisible in the terminal output.

</back>
